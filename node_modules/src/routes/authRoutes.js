//require express library
const express = require('express');
//create router using express lib
const router = express.Router();// can use it as a handler (post etc)

//anytime someone makes a post request to '/sign up'- we want to run function with rec and rez objects:
//see if anyone is signed up with that email before -> have some error handling and -> eventually synthesize that Jason web token and send it back to the phone:

router.post('/signup', (req, res) => {
    res.send('You made a post request');

});
//export the router to use it in the app (other parts) 
module.exports = router;